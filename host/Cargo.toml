[package]
name = "emacs-hypervisor"
version = "0.1.1"
edition = "2021"
publish = false

[dependencies]
clap = { version = "4.5", features = ["derive"] }
elle = { path = "../.elle" }

[build-dependencies]
elisp_pack = { path = "elisp_pack" }

[package]
name = "elle-mmdflux"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
elle-plugin = { path = "../../.elle/elle-plugin" }
mmdflux = { version = "2.5.0", default-features = false }
unicode-width = "0.2"
